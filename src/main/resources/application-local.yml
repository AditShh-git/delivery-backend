spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/${POSTGRES_DB}?options=-c%20TimeZone=Asia/Kolkata
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  data:
    redis:
      host: localhost
      port: 6379